{
  "name": "employee-picker",
  "version": "1.0.0",
  "description": "Roll a random Recursionaut",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "html-to-json": "^0.6.0",
    "slackbots": "^1.0.2"
  }
}
